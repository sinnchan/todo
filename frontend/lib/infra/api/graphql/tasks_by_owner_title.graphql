query TasksByOwnerTitle(
  $owner: String!
  $sortDirection: ModelSortDirection
  $limit: Int
  $nextToken: String
) {
  tasksByOwnerTitle(
    owner: $owner
    sortDirection: $sortDirection
    limit: $limit
    nextToken: $nextToken
  ) {
    items {
      id
      owner
      title
      description
      datetime
      isCompleted
      createdAt
      updatedAt
    }
    nextToken
  }
}
